# Node MVC Fastify Template

### Def:

 - `POST` -> Create (DB)
 - `PUT` -> Update (DB)
 - `GET` -> Get (DB)
 - `DELETE` -> Remove (DB)

>`cp .env-example .env`
>`code .`
SET env variables
>`npm install`
>`npm start`
